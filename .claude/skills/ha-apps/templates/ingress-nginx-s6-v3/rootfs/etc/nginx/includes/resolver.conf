# ==============================================================================
# DNS resolver configuration
# Uses Docker's embedded DNS server
# ==============================================================================

resolver 127.0.0.11 ipv6=off valid=30s;
