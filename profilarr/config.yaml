---
name: "Profilarr"
version: "1.0.9"
slug: "profilarr"
description: |
  Profile manager for Radarr and Sonarr instances.
  Centrally manage quality profiles, custom formats, and
  release profiles with Git-backed configuration storage.
url: https://github.com/rigerc/home-assistant-addons
arch:
  - aarch64
  - amd64
init: false
startup: services
boot: manual
ingress: true
ingress_port: 8099
ingress_entry: profilarr
ports:
  6868/tcp: 6868
ports_description:
  6868/tcp: "Web UI and API (optional, ingress is primary access method)"
panel_icon: "mdi:cog-transfer"
webui: "http://[HOST]:[PORT:6868]"
map:
  - share:rw
  - addon_config:rw
options:
  auth_mode: "on"
  log_level: "info"
schema:
  auth_mode: list(on|local|oidc|off)
  log_level: list(trace|debug|info|warning|error)
image: "ghcr.io/rigerc/ha-addons-profilarr-{arch}"
