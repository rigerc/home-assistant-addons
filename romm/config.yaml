---
name: "Romm"
version: "1.0.0"
slug: "romm"
description: |
  Self-hosted ROM collection manager and emulator launcher.
  Scan, organize, and manage game collections across 400+ platforms with
  automatic metadata fetching and in-browser gameplay.
arch:
  - aarch64
  - amd64
image: "ghcr.io/bluemaex/ha-addons-romm-{arch}"
startup: system
boot: auto
url: https://github.com/bluemaex/home-assistant-addons/tree/master/romm
init: false
ingress: true
ingress_port: 8080
panel_icon: mdi:gamepad-variant
ports:
  8080/tcp: null
ports_description:
  8080/tcp: Romm Web UI (disabled - use ingress)
map:
  - share
  - config
options:
  log_level: info
  database:
    host: ""
    port: 3306
    name: "romm"
    user: "romm-user"
    password: ""
  auth_secret_key: ""
  metadata_providers:
    screenscraper_user: ""
    screenscraper_password: ""
    retroachievements_api_key: ""
    steamgriddb_api_key: ""
    igdb_client_id: ""
    igdb_client_secret: ""
    mobygames_api_key: ""
    hasheous_enabled: true
    playmatch_enabled: true
    launchbox_enabled: true
  library_path: "/share/roms"
  env_vars: []
schema:
  log_level: list(trace|debug|info|notice|warning|error|fatal)
  database:
    host: str
    port: "int(1,65535)"
    name: str
    user: str
    password: password
  auth_secret_key: str
  metadata_providers:
    screenscraper_user: str?
    screenscraper_password: password?
    retroachievements_api_key: password?
    steamgriddb_api_key: password?
    igdb_client_id: str?
    igdb_client_secret: password?
    mobygames_api_key: password?
    hasheous_enabled: bool
    playmatch_enabled: bool
    launchbox_enabled: bool
  library_path: str
  env_vars:
    - str?
