#!/usr/bin/with-contenv bashio
exit_code=${1}

bashio::log.warning "File watcher exited with code ${exit_code}"

if [ "${exit_code}" -ne 0 ]; then
    bashio::log.error "File watcher crashed - stopping add-on"
    /run/s6/basedir/bin/halt
fi
