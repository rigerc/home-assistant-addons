#!/usr/bin/with-contenv bashio
# shellcheck shell=bash

declare APP_EXIT_CODE=${1}

bashio::log.info "Stopping RQ worker..."

if [ -f /tmp/rq_worker.pid ]; then
    kill $(cat /tmp/rq_worker.pid) 2>/dev/null || true
    rm -f /tmp/rq_worker.pid
fi
