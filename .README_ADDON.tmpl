{{- $repo := .Env.REPOSITORY -}}
{{- $repo_url := .Env.REPOSITORY_URL -}}
{{- $slug := .Env.ADDON_SLUG -}}
{{- $addons := .Env.ADDONS_DATA | jsonArray -}}
{{- range $addons -}}
{{- if eq .slug $slug -}}
{{- $addon := . -}}

# {{ $addon.name }}

{{ if $addon.upstream_image_repo }}![Upstream Status](https://img.shields.io/endpoint?url=https://raw.githubusercontent.com/{{ $repo }}/main/.versions/{{ $addon.slug }}.json){{ end }} ![Add-on Version](https://img.shields.io/badge/version-{{ $addon.version | strings.ReplaceAll "-" "--" }}-blue)

{{ $addon.description }}

## About

Home Assistant add-on for [{{ $addon.name }}]({{ $addon.project_url }}).

## Installation

1. In Home Assistant, go to **Settings** → **Add-ons** → **Add-on Store**
2. Click the three dots (⋮) in the top right corner
3. Select "Add repository from URL"
4. Enter: `https://github.com/{{ $repo }}`
5. Click "Add"
6. Find "{{ $addon.name }}" in the list and click it
7. Click "Install"

## Supported Architectures

Our add-on supports the following architectures with manifests.

{{ range $addon.arch }}- `{{ . }}`
{{ end }}

## Configuration

The add-on can be configured via the Home Assistant UI. See the upstream project for configuration details.

## Support

- **Documentation**: [DOCS.md]({{ $repo_url }}/blob/main/{{ $addon.slug }}/DOCS.md)
- **Changelog**: [CHANGELOG.md]({{ $repo_url }}/blob/main/{{ $addon.slug }}/CHANGELOG.md)
- **Issues**: Report issues on [GitHub]({{ $repo_url }})
- **Upstream**: [{{ $addon.name }}]({{ $addon.project_url }})

## License

This add-on is provided as-is.
{{- end -}}
{{- end -}}